# =============================================================================
# Nginx — 3F Hub (Huly)
# Domínio: 3ftasks.3fventure.tech
#
# Como aplicar na VPS:
#   cp /opt/apps/os-tasks/dev/nginx/3f-hub.conf /etc/nginx/sites-available/3f-hub
#   ln -s /etc/nginx/sites-available/3f-hub /etc/nginx/sites-enabled/3f-hub
#   nginx -t && systemctl reload nginx
# =============================================================================

# Redireciona HTTP → HTTPS
server {
    listen 80;
    server_name 3ftasks.3fventure.tech;
    return 301 https://$host$request_uri;
}

# Frontend principal (HTTPS porta 443)
server {
    listen 443 ssl;
    server_name 3ftasks.3fventure.tech;

    ssl_certificate /etc/letsencrypt/live/3ftasks.3fventure.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/3ftasks.3fventure.tech/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://localhost:8087;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }
}

# Account API (porta 3000)
server {
    listen 3000 ssl;
    server_name 3ftasks.3fventure.tech;

    ssl_certificate /etc/letsencrypt/live/3ftasks.3fventure.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/3ftasks.3fventure.tech/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_hide_header "Access-Control-Allow-Credentials";
        proxy_hide_header "Access-Control-Allow-Origin";

        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Real-IP $remote_addr;

        add_header "Access-Control-Allow-Origin" "https://3ftasks.3fventure.tech" always;
        add_header "Access-Control-Allow-Credentials" "true" always;
        add_header "Access-Control-Allow-Headers" "Content-Type, Authorization, x-timezone" always;
        add_header "Access-Control-Allow-Methods" "GET, POST, OPTIONS, PUT, DELETE" always;

        if ($request_method = OPTIONS) {
            return 204;
        }
    }
}

# Transactor WebSocket (porta 3332)
server {
    listen 3332 ssl;
    server_name 3ftasks.3fventure.tech;

    ssl_certificate /etc/letsencrypt/live/3ftasks.3fventure.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/3ftasks.3fventure.tech/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://localhost:3332;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }
}

# Collaborator WebSocket (porta 3078)
server {
    listen 3078 ssl;
    server_name 3ftasks.3fventure.tech;

    ssl_certificate /etc/letsencrypt/live/3ftasks.3fventure.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/3ftasks.3fventure.tech/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://localhost:3078;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }
}
